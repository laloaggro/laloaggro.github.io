{
  "name": "cv-portfolio",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm run servidor\" \"npm run cliente\"",
    "servidor": "cd page3/server && npm run dev",
    "cliente": "http-server page3 -p 8080",
    "instalar": "npm install && cd page3/server && npm install",
    "limpiar": "rm -rf node_modules package-lock.json && cd page3/server && npm run limpiar"
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "http-server": "^14.1.1"
  }
}
